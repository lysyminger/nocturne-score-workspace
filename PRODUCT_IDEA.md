# 视频乐谱转在线互动谱平台：产品与技术构想

> 状态：想法记录 / v0.1  
> 日期：2026-08-22  
> 说明：这是项目早期的产品与技术构想，部分阶段已经实现；当前能力以 [README.md](./README.md) 和 [RECOGNITION.md](./RECOGNITION.md) 为准。

## 1. 一句话定义

用户上传或导入一个“画面中持续展示/滚动乐谱”的视频，系统从视频中还原结构化的电吉他谱或钢琴谱；用户可在网页中播放、变速、循环和跟练，并导出 PDF、MusicXML、MIDI 以及 Guitar Pro `.gp` 谱，之后还可把谱面项目发布到社区。

本文中的“GTP 谱”暂按“Guitar Pro 可打开的谱文件”理解。第一目标格式建议使用 Guitar Pro 7 的 `.gp`，不要把旧 `.gp5` 作为首期硬性目标。

## 2. 可行性结论

整体可行，但不能在第一版承诺“任意视频一键 100% 转谱”。应先把输入范围限制为：

- 谱面位置基本固定；
- 谱面主要做上下滚动，旋转和透视变化很小；
- 分辨率足够，五线、TAB 线、数字和符号清晰；
- 手、人物、弹幕、歌词和播放控件不会长期挡住谱面；
- 首期只支持少量明确的谱面模板和常见记号。

在这个范围内，最有希望先实现的是“纵向滚动电吉他六线谱视频 → 可校正的在线谱”。标准钢琴五线谱可以作为下一阶段。下落音符、Synthesia、真人拍纸质乐谱、镜头不断移动的视频属于不同识别问题，不应和首期共用同一个承诺。

真正的难点分成两层：

1. 视频重建：从连续帧中拼出不重复、无播放光标干扰的完整谱面图。
2. 乐谱理解：把图像中的谱表、节拍、音符、品位和技巧记号恢复成能播放、编辑和导出的结构化数据。

网页显示、播放、循环练习和 `.gp` 导出已有较成熟的技术基础；识别准确率和人工校正体验才是产品成败点。

## 3. 对现有算法想法的修正

原始想法是：固定截取视频的一块区域，程序自动判断上下移动了多少小节，遇到重复画面时根据上下内容判断是否继续。

方向正确，但处理顺序应改成：

```text
视频
  → 选择谱面区域 ROI
  → 抽取清晰帧
  → 估计相邻帧的垂直像素位移
  → 重叠区域匹配与去重
  → 拼接完整长谱图
  → 检测谱表、谱行和小节线
  → 识别音符/TAB/节奏/技巧
  → 人工校正
  → 在线练习与多格式导出
```

不要在原始视频帧阶段直接判断“移动了几个小节”。因为此时可能还不知道小节线在哪里，而且同一小节在不同视频中的像素高度、缩放比例和滚动速度都不相同。应先求“移动了多少像素”，完成拼接后再检测谱行和小节。

### 3.1 视频重建算法

1. 让用户在第一帧手动框选谱面区域，系统可以给出自动建议，但首期保留人工确认。
2. 按自适应频率抽帧，不必逐帧处理；画面静止时少取，开始滚动时提高取样率。
3. 对 ROI 做灰度化、对比度增强和必要的透视校正。
4. 屏蔽固定水印、播放按钮、进度条和移动播放光标等干扰区域。
5. 使用相位相关、模板匹配、特征匹配或稠密光流，估计相邻帧的主要垂直位移。
6. 从候选帧中选择清晰度最高、遮挡最少的一帧，而不是机械地使用每一帧。
7. 根据重叠区域相似度，只把新出现的顶部或底部条带追加到长图中。
8. 当匹配度突然降低时，判定为换页、跳转、缩放变化、镜头切换或异常，建立新的片段，不强行拼接。
9. 最后对各片段做全局对齐、重复段检测和人工预览确认。

### 3.2 “重复”必须分成三类

- 滚动造成的帧间重叠：属于图像重复，应自动去掉。
- 视频回放、拖动进度或同一页再次出现：属于时间轴重复，系统应提示用户选择“忽略再次出现”或“按演奏顺序保留”。
- 乐曲本身的反复记号：属于音乐结构，不能简单删除，应保留反复记号、房子和跳转关系，并决定网页播放时是否展开。

仅使用整帧感知哈希容易把“长得相似的两个小节”误判成同一段。更稳妥的方式是同时使用：局部重叠位置、垂直位移连续性、谱行边界、小节特征和视频时间顺序。

## 4. 识别范围设计

### 4.1 首期电吉他谱

建议先支持：

- 六线 TAB；
- 拍号、小节线、反复记号；
- 品位数字、休止、连音和基础时值；
- 基础技巧：击弦、勾弦、滑音、推弦、闷音、泛音；
- 调弦、变调夹、速度和音轨名称。

电吉他谱不能只识别数字。若视频只显示品位数字、不显示可靠节奏，系统只能还原“指法序列”，不能凭空生成准确时值。此时应明确标记为低置信度，允许用户结合视频时间点手动补拍子。

### 4.2 后续钢琴谱

钢琴谱通常需要同时处理高低音谱表、双手、多声部、和弦、连线、力度和踏板。现有 OMR 可以作为起点，但即使面对清晰印刷谱也仍需要人工校正，因此钢琴谱应在视频拼接闭环稳定后再加入。

### 4.3 暂不混入首期的输入

- Synthesia/瀑布流/下落音符视频；
- 真人演奏画面中偶尔出现的小谱框；
- 手写谱；
- 严重透视、晃动、反光、遮挡的视频；
- 仅凭演奏音频自动扒出完整多声部谱。

这些输入可以以后建立独立识别管线，不能简单复用“滚动谱面拼接 + OMR”。

## 5. 中间数据格式

不要把 PDF 或 Guitar Pro 文件直接当作数据库主数据。建议采用：

- 项目内部规范化 JSON：保存识别结果、置信度、人工修改、视频时间映射和版本历史；
- MusicXML：作为标准乐谱交换格式；
- `.gp`：作为 Guitar Pro 用户的导出格式；
- PDF：作为固定版式阅读/打印格式；
- MIDI：作为通用试听和练习交换格式。

核心对象可先定义为：

```text
ScoreProject
├─ sourceVideo：来源、时长、版权声明、文件或链接
├─ captureRegions：谱面区域、遮罩、时间范围
├─ reconstructedPages：拼接图、片段、帧和像素映射
├─ score：曲目、速度、拍号、调号、轨道、谱表、小节、声部、节拍、音符
├─ techniques：推弦、滑音、击勾弦、踏板等演奏信息
├─ confidence：页面/小节/音符/符号级置信度
├─ corrections：人工修改记录
├─ syncPoints：视频时间与小节/节拍映射
└─ publication：作者、来源、权限、社区状态和版本
```

这样以后更换识别模型、播放器或导出器时，不需要推翻整个项目。

## 6. 网页练习功能

首个可用版本应包含：

- 标准谱与 TAB 切换或上下联合显示；
- 播放光标跟随；
- 速度调节；
- A/B 小节循环；
- 节拍器和预备拍；
- 单轨播放、静音和音量；
- 指定小节开始；
- 视频原声与合成音切换或同步；
- 错误位置快速回到编辑器；
- 自动保存练习进度。

录音判定、麦克风音高检测、音频分离和 AI 演奏评分都可以以后增加，但不应阻塞首个闭环。

## 7. 导出设计

### PDF

从结构化谱重新排版后导出矢量 PDF，不能直接把拼接长图塞进 PDF。这样才能分页、缩放、打印并保持清晰。拼接长图只作为识别证据和校对底图。

### Guitar Pro

网页端可使用 alphaTab 的数据模型和 Guitar Pro 7 导出器生成 `.gp`。需要单独验证技巧记号、复拍子、多声部和特殊符号在 Guitar Pro 中能否无损打开。无法表达的内容应在导出前提示，而不是静默丢失。

### MusicXML 与 MIDI

MusicXML 应作为必备交换格式，便于进入 MuseScore 等编辑器。MIDI 适合试听和 DAW 使用，但它不能完整保存排版、TAB 指法和很多演奏记号，因此不能替代 MusicXML 或 `.gp`。

## 8. 社区边界

社区应建立在“识别、校正、练习、导出”已经跑通之后。首期社区只需要：

- 发布谱面项目；
- 曲名、乐器、难度、调弦、标签和来源；
- 收藏、评论、版本更新；
- 原作者/整理者署名；
- 原视频链接和时间点；
- 举报、下架、可见范围和版权声明。

不建议一开始永久保存所有原始视频。可以优先保存来源链接、用户授权上传的视频、必要的识别帧和谱面派生数据，以降低存储、转码、审核和版权压力。公开发布前必须让上传者确认其拥有处理和分享权限，并保留投诉与下架流程。

## 9. 推荐系统结构

```text
浏览器端
├─ 上传、ROI 与遮罩编辑
├─ 拼接结果校对
├─ 结构化谱编辑器
├─ alphaTab 乐谱渲染/光标/练习交互
├─ FluidSynth SoundFont 合成与播放
└─ PDF、MusicXML、MIDI、.gp 下载

服务端 API
├─ 项目、版本、用户和社区数据
├─ 文件签名上传与访问控制
└─ 异步任务状态与识别结果

异步识别 Worker
├─ FFmpeg 抽帧
├─ OpenCV 位移估计、去重和拼接
├─ 乐谱版面分析
├─ TAB 专用识别
├─ 标准五线谱 OMR
└─ 结果规范化与置信度计算

存储
├─ PostgreSQL：结构化项目和社区数据
├─ 对象存储：视频、帧、拼接图和导出文件
└─ 任务队列：耗时识别任务
```

第一阶段可以全部本地运行，不必一开始就建设云端社区和大规模任务系统。

## 10. 分阶段路线

截至 2026-08-23，在线原型已经完成阶段 1 的前半段：B 站视频获取、手动时间范围、画面 ROI 拖拽框选、按源帧间隔裁剪、候选帧预览与配置持久化。尚未完成的关键部分仍是帧间位移估计、去重、长图拼接以及人工删除/排序片段；界面会把当前结果明确称为“候选帧”。

### 阶段 0：算法验证，不做完整网站

- 收集 20～50 个同类纵向滚动 TAB 视频，并确保有权用于测试；
- 手工标注 ROI、谱行、小节和正确谱面；
- 完成抽帧、垂直位移估计、去重和长图拼接；
- 输出带调试信息的拼接图和失败原因；
- 验证不同速度、分辨率、光标和轻微遮挡。

通过标准：限定模板的视频中，大部分可以不丢行、不重复行地还原完整长图；失败样本能明确标记，不能悄悄输出错误结果。

### 阶段 1：最小在线闭环

- 上传视频；
- 手动框选 ROI 和遮罩；
- 后台生成拼接长图；
- 用户调整片段顺序、删除重复段；
- 先人工录入或半自动识别少量 TAB；
- 网页显示、播放、变速和循环；
- 保存项目 JSON。

### 阶段 2：TAB 结构化识别与导出

- 谱行、小节、弦线和品位数字识别；
- 基础节奏与技巧识别；
- 音符级置信度和网页校正；
- 导出 MusicXML、MIDI、PDF 和 `.gp`；
- 用 Guitar Pro、MuseScore 和网页播放器交叉打开验证。

### 阶段 3：钢琴谱与视频同步

- 接入标准五线谱 OMR；
- 支持钢琴大谱表、多声部；
- 记录视频时间与小节位置的同步点；
- 原视频、合成播放和光标同步练习。

### 阶段 4：社区

- 账号、发布、收藏、评论、标签和版本；
- 权限、举报、审核和下架；
- 热门谱、练习记录和创作者主页；
- 在真实使用量出现后再做推荐算法。

## 11. 必须量化的验证指标

- 拼接覆盖率：正确谱面行占全部谱面行的比例；
- 重复率：错误重复加入的谱面区域比例；
- 顺序错误率：谱行或小节次序错误数量；
- 小节检测准确率；
- 音符/TAB 字符错误率；
- 节奏和技巧记号准确率；
- 单曲人工校正时间；
- 导出的 `.gp`、MusicXML、MIDI 是否能被目标软件重新打开；
- 低置信度是否真正覆盖大部分错误，而不是给错误结果虚假的高分。

“识别成功”不能只看生成了文件，必须看谱是否按正确顺序、能正确播放、能编辑、能重新导入。

## 12. 主要风险与处理原则

1. 输入风格差异太大：首期建立模板白名单和输入质量检测。
2. TAB 缺少节奏信息：允许生成不完整草稿并要求人工补充，不猜造时值。
3. 相似小节被误删：重复检测必须结合位置、时间和局部结构，并提供撤销。
4. 视频光标污染谱面：多帧融合，选择同一区域未被光标覆盖的像素。
5. OMR 不可能完全准确：把校正器和置信度视为核心功能，而不是补丁。
6. `.gp` 语义丢失：建立导出兼容性用例，无法表达时明确警告。
7. 视频存储和转码成本高：本地优先、限制大小、异步处理、按需保留源文件。
8. 版权和社区内容风险：记录来源和授权、控制可见性、提供举报与下架流程。

## 13. 当前产品决策

- 先做固定 ROI 的纵向滚动 TAB 视频，不承诺通吃所有视频。
- 先重建像素长图，再识别行、小节和音符。
- 保留人工选区、片段排序和音符校正，自动化逐步提高。
- 内部 JSON + MusicXML 为长期数据边界，`.gp` 和 PDF 是导出物。
- 在线练习闭环优先于社区；社区最后接入。
- 任何识别失败都应显示置信度和原因，不能伪装成成功。

## 14. 下一步建议

下一步不应立刻搭建社区页面，而应做一个很小的“视频拼谱实验器”：上传一个符合限制的纵向滚动 TAB 视频，手动框选谱面区域，输出带帧来源标记的完整长图，并允许人工删除重复片段。只要这一步稳定，后面的 OMR、在线练习和导出才有可靠输入。

## 15. 技术依据

- alphaTab 官方文档：支持在网页中渲染标准谱、吉他 TAB、播放、变速、循环和外部音视频同步；支持导入 Guitar Pro/MusicXML，并可导出 Guitar Pro 7 `.gp`。
  - https://www.alphatab.net/docs/introduction
  - https://www.alphatab.net/docs/guides/exporter
- Audiveris 官方手册：可将清晰印刷五线谱做 OMR 并输出 MusicXML，但官方同样强调准确率并不完美，需要人工验证和校正。
  - https://audiveris.github.io/audiveris/_pages/handbook/
